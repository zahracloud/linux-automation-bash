# System Reporting Tools
Providing instant, professional visibility into server health.

### Scripts:
- **info.sh**: A real-time diagnostic report.
  - **Features:** Uses `uptime -p` (prtty) and `free -h` (human-readable) for professional formatting.
  - **Standardization:** Uses ISO-8601 date formatting (`YYYY-MM-DD`) for universal log compatibility.
